import mysql.connector

arguments = {
    "db": "practicals",
    "host": "localhost",
    "port": 3306,
    "password": "Kartikey02@",
    "user": "hitmonlee",
}


with mysql.connector.connect(**arguments) as cnx:
    cur = cnx.cursor()

    cur.execute("select * from Student")

    rows = cur.fetchall()

		print(rows)

